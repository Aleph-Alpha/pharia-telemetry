# Development dependencies for pharia-telemetry
# Note: This project uses uv for dependency management.
# Use 'uv sync --group dev' instead of pip install -r requirements-dev.txt
# This file is kept for reference only.

# Modern tooling (via uv dependency-groups)
ruff>=0.9.2
mypy>=1.5.0
pre-commit>=3.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
coverage>=7.0.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0
myst-parser>=2.0.0
